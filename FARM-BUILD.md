# Farm build intel

Why a build produced no packages, how `grokbuild-auto.sh` is supposed to
work, and the rundpb/sccache footguns:

**`~/git/sw/gb-support/BUILD-DRIVER.md`**

Troubleshooting: `~/git/sw/gb-support/GB-TROUBLESHOOTING-GUIDE.md`

Live wrapper: `~/bin/grokbuild-auto.sh` → `scripts/grokbuild-auto.sh`
(same inode as `~/git/sw/gb-support/scripts/grokbuild-auto.sh`).
